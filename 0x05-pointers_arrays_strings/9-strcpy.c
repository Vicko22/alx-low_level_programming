#include <string.h>
#include "main.h"

/**
* _strcpy - copy str from src to dest
* @dest: pointer to the destination str
* @src: pointer to the str to be copied9
* Return: pointer to the char with str copied (dest,)
*/

char *_strcpy(char *dest, char *src)

{

strcpy(dest, src);

return (dest);

}
